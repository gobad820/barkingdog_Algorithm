#include <bits/stdc++.h>
using namespace std;
#define endl "\n"
int n;
int board[15][15];
bool isused1[30];
bool isused2[30];
void func(int cur) {
    if (cur == n) {
    }
    for(int )
}
int main(int argc, char const *argv[]) {
    ios::sync_with_stdio(false);
    cin.tie(NULL);
    cin >> n;
    for (int i = 0; i < n; i++)
        for (int j = 0; j < n; j++) cin >> board[i][j];

    return 0;
}