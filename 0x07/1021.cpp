#include <bits/stdc++.h>
/* header */
using namespace std;
#define endl "\n"
int n, m;
int main(int argc, char const *argv[])
{
    ios::sync_with_stdio(false);
    cin.tie(NULL);
    cin >> n >> m;
    deque<int> dq;
    int arr[m + 5];
    for (int i = 0; i < m; i++)
        cin >> arr[i];

    for (int j = 1; j <= 10; j++)
    {
        dq.push(j);
    }
    for (int i = 0; i < m; i++)
    {
        if (arr[i] ==)
    }

    return 0;
}